In `./src` folder, find Kotlin transform processor and interface. Leverage data class to build data transformation.

* `model/input` folder contains input data classes
* `model/output` folder contains output data classes
 
- [ ] Step 1: In FlinkTransformOperatorTest, implement at test function that leverage input class and transform to output class.
